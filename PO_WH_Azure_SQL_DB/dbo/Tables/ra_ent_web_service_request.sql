CREATE TABLE [dbo].[ra_ent_web_service_request] (
    [id]        VARCHAR (255) NOT NULL,
    [count]     INT           NOT NULL,
    [timestamp] DATETIME      NULL,
    PRIMARY KEY CLUSTERED ([id] ASC)
);

